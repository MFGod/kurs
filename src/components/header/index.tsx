import { Button } from '../button';

export const Header = () => {
  return (
    <header>
      <Button text="Смена языка" />
      <Button text="Смена темы" />
      <Button text="Cтарт" />
      <Button text="Пауза" />
    </header>
  );
};
